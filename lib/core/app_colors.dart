import 'package:flutter/material.dart';

class AppColors {
  // 🔥 Brand Colors
  static const Color primary = Color(0xFF22C55E);   // Main brand GREEN
  static const Color accent  = Color(0xFFFF7A2F);   // Secondary / highlight ORANGE
  // 🎨 Backgrounds & Surfaces
  static const Color background = Color(0xFFF7F8FA);   // App background
  static const Color surface = Colors.white;           // Cards, sheets

  // ✍️ Text Colors
  static const Color textPrimary = Color(0xFF1C1C1C);
  static const Color textSecondary = Color(0xFF8A8A8A);
  static const Color textHint = Color(0xFFB5B5B5);

  // 🚨 States
  static const Color error = Color(0xFFE74C3C);
  static const Color warning = Color(0xFFF39C12);

  // 🧊 Misc
  static const Color divider = Color(0xFFEAEAEA);
}